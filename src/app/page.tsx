import { Button } from "@/components/ui/button";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <Button className=""> Salut tout le monde!! </Button>;
};

export default page;
